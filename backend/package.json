{
  "name": "sqlmind-backend",
  "version": "1.0.0",
  "description": "Python FastAPI Backend for SQLMind AI",
  "scripts": {
    "start": ".venv\\Scripts\\python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "dev": ".venv\\Scripts\\python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
  }
}
